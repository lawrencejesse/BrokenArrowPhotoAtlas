{
  "name": "photo-log-atlas-builder",
  "version": "1.0.0",
  "description": "Photo Log Atlas Builder — Broken Arrow Consulting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "analytics:weekly": "node scripts/ga4-weekly-report.js",
    "test": "node --test",
    "check": "node --check app.js && node --check server.js && node --check script.js && node --check accounts.js"
  },
  "dependencies": {
    "@clerk/express": "^2.1.49",
    "express": "^5.2.1",
    "pg": "^8.22.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.14"
  }
}
